import React from 'react';
import './Loading.css';

const Loading = () =>{

  return <div className="Loading" />

}

//again we do default here so that we can use it in other components 
export default Loading;
